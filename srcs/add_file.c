#include "../libft/libft.h"
#include "../includes/ft_ls.h"

void	ft_get_full_path(t_ls *file_data)
{

}

t_ls	ft_create_files()
{

}

void	ft_add_files(char *path, t_list **begin_list)
{
	t_list		*tmp;
	t_ls		*file_data;
	struct stat	file_tmp

	tmp = *begin_list;
	while (tmp)
		tmp = tmp->next;
	ft_memset(&file, 0, sizeof(t_ls));
}
